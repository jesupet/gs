import { createStore } from 'vuex'
import services from './services.json'

export default createStore({
  state: {
    services: services,
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
  }
})
